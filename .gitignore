# Build output
/target

# Locally captured AxonHub payloads.
# 请求.txt is a cURL capture whose Authorization header contains a real JWT,
# so it must never be committed. 响应.json is a response sample kept alongside
# it; both stay local as test fixtures.
/请求.txt
/响应.json

# Local profile data. credentials.bin holds DPAPI-encrypted secrets and must
# never be committed; config.json can leak an endpoint, so keep it out too.
/credentials.bin
/config.json
