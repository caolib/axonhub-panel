# Build output
/target

# Local profile data. credentials.bin holds DPAPI-encrypted secrets and must
# never be committed; config.json can leak an endpoint, so keep it out too.
/credentials.bin
/config.json
